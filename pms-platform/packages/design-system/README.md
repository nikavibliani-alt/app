# `@pms/design-system`

CSS tokens + Phase 0 primitives (`Button`, `Input`, `Card`, `Nav`).

**Preview (no install):** open `preview/index.html`

See `DESIGN.md` for palette rules. See `../../PHASE0.md` for Cursor / Claude Code handoff.
